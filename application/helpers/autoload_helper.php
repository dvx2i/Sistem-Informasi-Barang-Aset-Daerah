<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

if ( ! function_exists('getRole'))
{
  function getRole($parameter){
    // $role = json_decode($_SESSION['session']->role);
    // if( count(array_intersect($role, $parameter)) > 0 ){
    //     return true;
    // }else {
    //     return false;
    // }
  };
}
